import 'package:rec/Entities/Forms/FormData.dart';

class UpdateOfferData extends FormData {
  UpdateOfferData();

  // bool get isValid => Checks.isNotEmpty(content) && Checks.isNotEmpty(kind_id);

  @override
  Map<String, dynamic> toJson() {
    return {};
  }
}
